<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme_noActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#ffffff</item>
        <item name="colorPrimaryDark">#263238</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ExpandedAppBar" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="CollapsedAppBar" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">#263238</item>
        <item name="android:textSize">20dp</item>
    </style>


    <!-- TextSwitcher Style -->
    <style name="IdTextView">
        <item name="android:padding">3dp</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">@color/text_id</item>
        <item name="android:textStyle">italic|bold</item>
        <item name="android:gravity">left</item>
    </style>

    <style name="TagTextView">
        <item name="android:padding">3dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/text_tag</item>
        <item name="android:textStyle">italic</item>
        <item name="android:gravity">left</item>
    </style>

    <style name="ContentTextView">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/text_content</item>
        <item name="android:gravity">left</item>
    </style>

    <!-- food -->
    <style name="Ratingbar" parent="Base.Widget.AppCompat.RatingBar.Small"/>

    <!-- food review edittext -->
    <style name="ReviewEditText" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">@color/black_overlay</item>
        <item name="colorControlNormal">@color/black_overlay</item>
        <item name="colorControlActivated">@color/app_color</item>
    </style>
</resources>
